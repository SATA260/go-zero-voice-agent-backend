Name: llmservice.rpc
ListenOn: ${LLM_RPC_LISTEN}
Etcd:
  Hosts:
  - ${ETCD_HOST}
  Key: llmservice.rpc
DB:
  DataSource: ${LLM_DB_DSN}

Cache:
  - Host: ${REDIS_HOST}
    Type: node
    Pass: ${REDIS_PASS}

Redis:
  Host: ${REDIS_HOST}
  Type: node
  Pass: ${REDIS_PASS}
  Key:  

Asynq:
  Host: ${REDIS_HOST}
  Pass: ${REDIS_PASS}
  DB: 1

Timeout: ${LLM_RPC_TIMEOUT}
