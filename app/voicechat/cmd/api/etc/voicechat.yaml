Name: voiceChat
Host: ${VOICECHAT_API_HOST}
Port: ${VOICECHAT_API_PORT}

LlmRpcConf:
  Etcd:
    Hosts:
    - ${ETCD_HOST}
    Key: llmservice.rpc

VoiceChatRpcConf:
  Etcd:
    Hosts:
    - ${ETCD_HOST}
    Key: voicechat.rpc

RustPBXConfig:
  Url: ${RUST_PBX_URL}
  WebSocketUrl: ${RUST_PBX_WEBSOCKET_CALL_URL}
