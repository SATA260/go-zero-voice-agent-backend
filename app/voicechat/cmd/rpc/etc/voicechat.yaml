Name: voicechat.rpc
ListenOn: ${VOICECHAT_RPC_LISTEN}
Etcd:
  Hosts:
  - ${ETCD_HOST}
  Key: voicechat.rpc
DB:
  DataSource: ${VOICECHAT_DB_DSN}

Cache:
  - Host: ${REDIS_HOST}
    Type: node
    Pass: ${REDIS_PASS}
