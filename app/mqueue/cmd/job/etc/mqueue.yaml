Name: mqueue.job
Mode: ${MQUEUE_MODE}
Log:
  ServiceName: mqueue.job
  Mode: ${MQUEUE_LOG_MODE}
  Encoding: ${MQUEUE_LOG_ENCODING}
Redis:
  Host: ${REDIS_HOST}
  Type: node
  Pass: ${REDIS_PASS}

Asynq:
  Host: ${REDIS_HOST}
  Pass: ${REDIS_PASS}
  DB: 1

DB:
  DataSource: ${LLM_DB_DSN}
Cache:
  - Host: ${REDIS_HOST}
    Type: node
    Pass: ${REDIS_PASS}
